//
//  Foundation.swift
//  TestUIKit
//
//  Created by Duck Sern on 17/2/26.
//

import Foundation

/// Collection diffing
/// let diff = bird.difference(from: bear)
/// let newBeard =
///
/// struct Data is contigous buffer type
/// `ContigouseBytes` protocol
/// `DataProtocol` ~ [UInt8]
/// `data.compressed(using: .lzfse)`
/// `RelativeDateTimeFormatter`
/// `ListFormatter` + itemFormatter property
///
/// OperationQueue
/// - `addBarrierBlock` guarantee all job before done and only 1 operation barrier run
/// - Preogress reporting `progress.totalUniCount`
///
/// Swift
/// - scanner API:.scanUpToCharacters, scanString
