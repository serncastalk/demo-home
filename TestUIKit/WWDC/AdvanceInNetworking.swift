//
//  AdvanceInNetworking.swift
//  TestUIKit
//
//  Created by Duck Sern on 2/3/26.
//

import Foundation

/// Low data mode
/// - Defer backgorund task discretionary
/// - Backgorund app refersh is disabled
/// `constrained` keyword for `URLSession`,
/// `networkUnavailabelReason`
/// and NWParametersm NWPath => `expensive` keyword
///
/// `retry` operator in combine => not need recursive
/// Use NWListener for server use URLSessionWebSocketTask build on top network framework for client
/// Preflight network available is bad practice because wift assit
/// Control using `allowExpensiveNetworkAccess`
/// Multipath TCP transport => `multipathServiceType`
